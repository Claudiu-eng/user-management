variables:
  IMAGE_NAME: claudiutulbure/cicd
  IMAGE_TAG: user-image
build:
  image: docker:20.10.16
  services:
    - docker:20.10.16-dind
  script:
    - docker login -u $REGISTRY_USER -p $REGISTRY_PASS registry